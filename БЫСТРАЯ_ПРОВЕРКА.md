# ⚡ Быстрая проверка - ЧТО ДЕЛАТЬ СЕЙЧАС

## ✅ Сервер уже запущен!

Я открыл для вас 2 вкладки в браузере:

### 1️⃣ Главная бота
```
http://localhost:3000/
```
**Что должны увидеть:** Иконки игр (Roll, Crash, Mine и т.д.)

### 2️⃣ Сайт партнеров
```
http://localhost:3000/partner/
```
**Что должны увидеть:** Страницу "Duo Partners"

---

## 🔍 ЧТО ПРОВЕРИТЬ

### ✅ Если ВСЁ работает локально:

1. Стили загружаются
2. Игры открываются
3. Партнерский сайт открывается

**→ Значит проблема только на Render!**

**Решение:**
```bash
# Закоммитьте изменения
git add .
git commit -m "Fix routing for bot and partner site"
git push origin main
```

Render автоматически задеплоит → проверьте через 2-3 минуты.

---

### ❌ Если НЕ работает локально:

**Нажмите F12 в браузере** → вкладка **Console**

Скопируйте все ошибки (красные) и покажите мне!

Пример что искать:
```
❌ GET http://localhost:3000/pages_bot/main/style.css 404
❌ GET http://localhost:3000/config.js 404
```

---

## 🐛 Частые проблемы

### Проблема 1: Главная показывает не то

**Проверьте адрес:**
- `http://localhost:3000/` - бот
- `http://localhost:3000/partner/` - партнерка

### Проблема 2: 404 на все файлы

**Проверьте что сервер запущен из КОРНЯ:**
```bash
# ✅ Правильно
cd C:\Users\dev_fenomen\Desktop\bot
node server/server.js

# ❌ Неправильно
cd C:\Users\dev_fenomen\Desktop\bot\server
node server.js
```

### Проблема 3: Стили не загружаются

**Откройте:** `C:\Users\dev_fenomen\Desktop\bot\index.html`

Найдите строки с `<link rel="stylesheet"` и покажите мне пути.

---

## 📊 Проверка структуры файлов

Убедитесь что файлы на месте:

```
C:\Users\dev_fenomen\Desktop\bot\
├── index.html          ← Главная бота
├── config.js
├── balance-api.js
├── pages_bot\
│   └── main\
│       └── style.css   ← Стили бота
├── main\
│   └── index.html      ← Страница main
├── roll\
│   └── index.html      ← Игра roll
└── site\
    ├── index.html      ← Партнерка
    └── duo-partner\
        └── css\
            └── style.css
```

---

## 💬 Что сказать мне

Опишите что видите:

**Вариант A - Всё ОК:**
```
Локально всё работает! Бот открывается, стили загружаются, 
партнерка тоже показывается. Проблема только на Render.
```
→ Я помогу задеплоить правильно

**Вариант B - Не работает:**
```
Не работает: [опишите что именно]
Ошибки в консоли: [вставьте ошибки из F12]
```
→ Я помогу исправить

**Вариант C - Непонятно:**
```
Не понимаю что проверять / не открывается / показывает что-то не то
```
→ Я помогу разобраться пошагово

---

## 🎯 Главный вопрос

**Откройте эти 2 ссылки и скажите - они открываются?**

1. http://localhost:3000/ - видите иконки игр?
2. http://localhost:3000/partner/ - видите "Duo Partners"?

Если **ДА** → проблема только на Render, легко починить!
Если **НЕТ** → покажите что видите вместо этого.
