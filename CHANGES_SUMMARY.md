# 🎨 Обновления UI - Фильтры и Картинки

## Дата: 14.01.2026

### ✅ Исправлено

#### 1. **Фильтры кейсов (Main страница)** - ОБНОВЛЕНО
- ✅ Добавлена подсветка активного фильтра
- ✅ Плавная анимация переключения между фильтрами
- ✅ Красивая анимация индикатора `.select-filter` с эффектом bounce
- ✅ Эффект hover при наведении на фильтры
- ✅ Подсветка текста и иконок активного фильтра
- ✅ **ИСПРАВЛЕНО:** Переименован класс `.select` на `.select-filter` для избежания конфликтов с навигацией
- ✅ **ИСПРАВЛЕНО:** Увеличена ширина фильтров (370px → 400px)
- ✅ **ИСПРАВЛЕНО:** Текст фильтров больше не обрезается (All, Chips, New, Low, High)
- ✅ **ИСПРАВЛЕНО:** Индикатор больше не накладывается на другие элементы

**Стили:**
- Background с градиентом и backdrop-filter
- Box-shadow для глубины
- Transition с cubic-bezier для плавности
- Активный текст: белый цвет + тень
- Активные иконки: brightness + drop-shadow
- Flex-shrink: 0 для предотвращения сжатия элементов
- White-space: nowrap для предотвращения переноса текста

#### 2. **Картинка приза в Upgrade**
- ✅ Добавлена функция `updatePrizeDisplay()` 
- ✅ Картинка обновляется при нажатии Apply
- ✅ Картинка обновляется при изменении множителя
- ✅ Fallback на текст если картинка не загрузилась
- ✅ Стильный drop-shadow для картинки

### 📂 Измененные файлы

1. **bot/main/style.css**
   - `.select-filter` (строка 1685-1698) - новый класс для фильтров
   - `.select` (строка 1702-1709) - восстановлен для навигации
   - `.section-menu` - width: 400px, justify-content: space-between
   - `.text-wrapper-9/10/11/12` - width: auto, white-space: nowrap
   - Hover эффекты для фильтров
   - Активное состояние фильтров

2. **bot/index.html**
   - `<div class="select"></div>` → `<div class="select-filter"></div>` (строка 662)

3. **bot/main/home.js**
   - `.select` → `.select-filter` в селекторах (строка 155, 245)

4. **bot/upgrade/index.js**
   - Добавлена функция `updatePrizeDisplay()` (строка 358-384)
   - Обновлен обработчик Apply button (строка 421-423)
   - Обновлен обработчик множителей (строка 241-246)

### 🎯 Результат

**Main страница:**
- Фильтры теперь визуально показывают какой активен
- Плавная анимация при переключении
- Индикатор `.select` двигается под активный элемент

**Upgrade страница:**
- В центре колеса теперь показывается картинка приза
- Картинка обновляется динамически
- Красивый эффект тени

### 🚀 Деплой

Для применения изменений на сервере:

```bash
cd /var/www/duo
git pull origin main
pm2 restart duo-server
```

### 🎨 Preview

**Фильтры:**
- Неактивный: серый текст (#ffffffa8)
- Активный: белый текст (#ffffff) + тень + подсветка
- Индикатор: серовато-прозрачный фон с размытием

**Upgrade:**
- Картинка приза: token-2.png из GitHub
- Эффект: drop-shadow
- Fallback: текст с суммой приза
