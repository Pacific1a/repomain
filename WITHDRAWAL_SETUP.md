# 💸 Система ручных выплат - Инструкция по установке

Полная система ручной обработки заявок на вывод средств через Telegram бота.

## 📋 Что было создано

1. **База данных** - таблица `withdrawal_requests`
2. **API сервер** - endpoints для создания и обработки заявок
3. **Telegram бот** - для админов с кнопками одобрить/отклонить
4. **Фронтенд интеграция** - автоматическая отправка после 2FA

---

## 🚀 Установка на сервере

### Шаг 1: Обновить код

```bash
cd /var/www/duo
git pull origin main
```

### Шаг 2: Запустить миграцию базы данных

```bash
cd /var/www/duo/server
node migrations/add-withdrawal-requests.js
```

Должно быть:
```
✅ Таблица withdrawal_requests создана
✅ Индексы созданы
✅ Миграция успешно завершена!
```

### Шаг 3: Создать Telegram бота

1. Откройте Telegram → @BotFather
2. Отправьте `/newbot`
3. Введите название: `DuoPartners Withdrawal Bot`
4. Введите username: `duo_withdrawal_bot` (или любой свободный)
5. Скопируйте токен (например: `8172657942:AAEaHf7zEYNfs_KKIyed-HzvsUKYELgxnOw`)

### Шаг 4: Получить Telegram ID админов

1. Откройте @userinfobot в Telegram
2. Отправьте любое сообщение
3. Скопируйте ваш ID (например: `123456789`)
4. Повторите для всех админов

### Шаг 5: Сгенерировать секретный ключ

```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

Скопируйте результат (например: `a1b2c3d4e5f6...`)

### Шаг 6: Настроить .env сервера

Откройте `/var/www/duo/server/.env` и добавьте в конец:

```bash
# ============================================
# WITHDRAWAL BOT CONFIGURATION
# ============================================

# Withdrawal Bot API URL (где запущен withdrawal-bot)
WITHDRAWAL_BOT_URL=http://localhost:3002/api/withdrawal

# Секрет для связи с withdrawal-bot
# ВАЖНО: Должен совпадать с BOT_SECRET в withdrawal-bot/.env
WITHDRAWAL_BOT_SECRET=ВСТАВЬТЕ_СЕКРЕТ_ИЗ_ШАГА_5
```

### Шаг 7: Установить зависимости бота

```bash
cd /var/www/duo/withdrawal-bot
npm install
```

### Шаг 8: Настроить .env бота

```bash
cd /var/www/duo/withdrawal-bot
cp .env.example .env
nano .env
```

Заполните:

```bash
# Токен бота из @BotFather (Шаг 3)
WITHDRAWAL_BOT_TOKEN=8172657942:AAEaHf7zEYNfs_KKIyed-HzvsUKYELgxnOw

# Telegram ID админов через запятую (Шаг 4)
ADMIN_IDS=123456789,987654321

# Секрет из Шага 5 (ОДИНАКОВЫЙ с server/.env!)
BOT_SECRET=ВСТАВЬТЕ_СЕКРЕТ_ИЗ_ШАГА_5

# URL партнёрского сервера
SERVER_API_URL=http://localhost:3001

# Порт для API бота
PORT=3002
```

Сохраните: `Ctrl+O → Enter → Ctrl+X`

### Шаг 9: Запустить бота через PM2

```bash
pm2 start /var/www/duo/withdrawal-bot/index.js --name withdrawal-bot
pm2 save
```

Проверить:
```bash
pm2 list
pm2 logs withdrawal-bot
```

Должно быть:
```
🤖 Withdrawal Bot запущен!
👥 Админы: 123456789, 987654321
🌐 API сервер запущен на порту 3002
```

### Шаг 10: Перезапустить партнёрский сервер

```bash
pm2 restart duo-server
pm2 logs duo-server --lines 50
```

Должно быть:
```
✅ Withdrawal routes loaded
✅ Withdrawal admin routes loaded
```

---

## ✅ Тестирование системы

### 1. Проверка бота

```bash
# Откройте вашего бота в Telegram
# Отправьте /start
# Должно прийти: "Привет, админ! Я буду присылать сюда заявки на вывод средств."
```

### 2. Проверка API

```bash
curl http://localhost:3002/health
# Должно: {"status":"ok"}

curl http://localhost:3001/api/status
# Должно: {"success":true,"server":"DUO Unified Server"}
```

### 3. Полный flow

1. Зайдите на сайт партнёрской системы
2. Авторизуйтесь
3. Нажмите "Вывод средств"
4. Заполните сумму и USDT адрес
5. Пройдите 2FA верификацию
6. Введите OTP код
7. В Telegram боте админ должен получить сообщение:

```
🆕 НОВАЯ ЗАЯВКА НА ВЫВОД

💰 Сумма: 1000₽

👤 Информация о пользователе:
├ ID: 123
├ Email: user@example.com
├ Telegram: @username

📊 Статистика:
├ Рефералов: 5
├ Заработано: 3000₽

💳 USDT TRC20 адрес:
TXxx...xxx

🆔 Заявка #1
```

8. Админ нажимает "✅ Одобрить"
9. Баланс пользователя обнуляется (3000₽ → 0₽)
10. Статистика остаётся (рефералы, заработок)

---

## 🔧 Troubleshooting

### Проблема: Бот не отвечает

```bash
pm2 logs withdrawal-bot
# Смотрим ошибки

# Проверяем токен
cd /var/www/duo/withdrawal-bot
cat .env | grep BOT_TOKEN
```

### Проблема: Админ не получает сообщения

```bash
# Проверяем ID админа
cat .env | grep ADMIN_IDS

# Попробуйте получить ID заново через @userinfobot
```

### Проблема: "Invalid secret"

```bash
# Убедитесь что секреты одинаковые:
cat /var/www/duo/server/.env | grep WITHDRAWAL_BOT_SECRET
cat /var/www/duo/withdrawal-bot/.env | grep BOT_SECRET

# Должны быть ИДЕНТИЧНЫ!
```

### Проблема: Заявка не отправляется в бота

```bash
# Проверьте логи сервера
pm2 logs duo-server | grep withdrawal

# Проверьте что бот запущен
pm2 list | grep withdrawal-bot

# Проверьте URL в server/.env
cat /var/www/duo/server/.env | grep WITHDRAWAL_BOT_URL
# Должно быть: http://localhost:3002/api/withdrawal
```

---

## 📊 Структура базы данных

### Таблица: withdrawal_requests

| Поле | Тип | Описание |
|------|-----|----------|
| id | INTEGER | ID заявки |
| user_id | INTEGER | ID пользователя |
| email | TEXT | Email пользователя |
| telegram_username | TEXT | @username |
| amount | REAL | Сумма вывода |
| usdt_address | TEXT | USDT TRC20 адрес |
| referrals_count | INTEGER | Количество рефералов |
| total_earnings | REAL | Сколько заработал |
| status | TEXT | pending/approved/rejected |
| created_at | DATETIME | Дата создания |
| processed_at | DATETIME | Дата обработки |
| processed_by | TEXT | Кто обработал |
| admin_comment | TEXT | Комментарий админа |

---

## 🔒 Безопасность

1. **Секретный ключ** - проверяется во всех запросах между сервером и ботом
2. **Только админы** - только пользователи из ADMIN_IDS могут обрабатывать заявки
3. **JWT токены** - пользователи авторизуются через JWT
4. **2FA обязательна** - без 2FA нельзя создать заявку
5. **Транзакции** - обнуление баланса происходит атомарно
6. **Логирование** - все действия записываются в консоль и БД

---

## 📝 API Endpoints

### User endpoints (требуют JWT):

```
POST /api/withdrawal/request
Body: { amount: 1000, usdtAddress: "TXxx...xxx" }
Response: { success: true, requestId: 123 }

GET /api/withdrawal/history
Response: { success: true, requests: [...] }
```

### Admin endpoints (требуют X-Bot-Secret):

```
POST /api/withdrawal/approve
Headers: X-Bot-Secret: secret-key
Body: { requestId: 123, adminName: "@admin" }
Response: { success: true, newBalance: 0 }

POST /api/withdrawal/reject
Headers: X-Bot-Secret: secret-key
Body: { requestId: 123, adminName: "@admin", comment: "reason" }
Response: { success: true }
```

---

## 🔄 Обновление системы

```bash
cd /var/www/duo
git pull origin main
pm2 restart duo-server
pm2 restart withdrawal-bot
```

---

## 📈 Мониторинг

```bash
# Логи сервера
pm2 logs duo-server

# Логи бота
pm2 logs withdrawal-bot

# Список процессов
pm2 list

# Статус системы
pm2 status
```

---

## ❓ Частые вопросы

**Q: Можно ли добавить больше админов?**  
A: Да, добавьте их ID через запятую в ADMIN_IDS

**Q: Можно ли отменить одобренную заявку?**  
A: Нет, после одобрения баланс уже обнулён. Статус нельзя изменить.

**Q: Где хранятся заявки?**  
A: В базе данных `server/database.db` в таблице `withdrawal_requests`

**Q: Как посмотреть все заявки?**  
A: Подключитесь к БД:
```bash
cd /var/www/duo/server
sqlite3 database.db
SELECT * FROM withdrawal_requests;
```

**Q: Бот упал, что делать?**  
A: Перезапустите:
```bash
pm2 restart withdrawal-bot
pm2 logs withdrawal-bot
```

---

## 🎉 Готово!

Система ручных выплат настроена и готова к работе!

- ✅ База данных создана
- ✅ API работает
- ✅ Telegram бот запущен
- ✅ Фронтенд интегрирован

Пользователи могут создавать заявки, админы - одобрять или отклонять их прямо из Telegram!
