# Server Configuration
PORT=3000
NODE_ENV=development

# MongoDB (опционально - оставьте пустым для работы без БД)
# Для локальной разработки с MongoDB:
# MONGODB_URI=mongodb://localhost:27017/telegram_games
# Для продакшена (MongoDB Atlas):
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/telegram_games
MONGODB_URI=

# JWT Secret (для будущей авторизации)
JWT_SECRET=your_super_secret_key_change_this_in_production

# CORS
ALLOWED_ORIGINS=*

