# 🧪 РУКОВОДСТВО ПО ТЕСТИРОВАНИЮ РЕФЕРАЛЬНОЙ СИСТЕМЫ

## 📋 СОДЕРЖАНИЕ
1. [Подготовка к тестированию](#подготовка)
2. [Тестирование графика](#тест-графика)
3. [Тестирование переходов](#тест-переходов)
4. [Тестирование депозитов](#тест-депозитов)
5. [Тестирование проигрышей](#тест-проигрышей)
6. [Тестирование комиссии](#тест-комиссии)
7. [Сброс статистики](#сброс-статистики)

---

## 🔧 ПОДГОТОВКА К ТЕСТИРОВАНИЮ {#подготовка}

### 1. Убедись что все сервисы запущены:
```bash
ssh root@77.239.125.70

pm2 list
# Должны быть online:
# - duo-partner (порт 3000)
# - duo-casino (порт 3001)
# - duo-bot
```

### 2. Очисти статистику для чистого теста:
```bash
sqlite3 /var/www/duo/site/server/data/database.db

DELETE FROM referral_stats;
DELETE FROM referrals;
.quit
```

### 3. Перезапусти сервисы:
```bash
pm2 restart all
```

### 4. Открой сайт партнёра:
```
http://77.239.125.70
```

---

## 📊 ТЕСТ 1: ГРАФИК {#тест-графика}

### Что проверяем:
- ✅ Все линии видны и разделены (не накладываются)
- ✅ Tooltip показывает реальные значения (не с offset)
- ✅ При нулевых данных линии на разных уровнях
- ✅ Точки видны на линиях

### Шаги теста:

#### 1.1 Проверь начальное состояние (всё = 0):
```
Ожидаемый результат:
- Переходы: 0 → линия видна на уровне ~3px
- Доход: 0 → линия видна на уровне ~6px
- Депозиты: 0 → линия видна на уровне ~9px
- Первые депозиты: 0 → линия видна на уровне ~13px

ВСЕ ЛИНИИ РАЗДЕЛЕНЫ! (не накладываются)
```

#### 1.2 Наведи на точку любой линии:
```
Ожидаемый результат в tooltip:
- Переходы: 0 (не 3, не 13!)
- Доход: 0₽ (не 6₽, не 9₽!)
- Депозиты: 0₽
- Первые депозиты: 0₽
```

#### 1.3 Откройте DevTools Console (F12):
```javascript
// Должен быть лог:
📊 Chart Debug: {
    maxValue: 0,
    totalSpacing: 10,
    baseLift: 3,
    lineCount: 4,
    ...
}
```

✅ **ТЕСТ ПРОЙДЕН** если все 4 линии видны и tooltip показывает 0.

---

## 👥 ТЕСТ 2: ПЕРЕХОДЫ (CLICKS) {#тест-переходов}

### Что проверяем:
Регистрация перехода по реферальной ссылке.

### Шаги теста:

#### 2.1 Получи реферальную ссылку:
```
1. Открой http://77.239.125.70
2. Залогинься (admin_duo / 12345678)
3. Кликни "РЕФ.ПРОГРАММА"
4. Скопируй ссылку (должна быть вида):
   https://t.me/aasasdasdadsddasdbot?start=ref_XXXXX
```

#### 2.2 Перейди по ссылке в Telegram:
```
1. Открой ссылку в Telegram
2. Нажми /start в боте
3. Должно показаться сообщение:
   "🎁 Вы перешли по реферальной ссылке!
   Ваш партнёр будет получать 60% от ваших проигрышей."
```

#### 2.3 Проверь статистику на сайте:
```
Обнови страницу (Ctrl+F5)

Ожидаемый результат:
- Переходы: 1 ✅
- Первые депозиты: 0
- Кол-во пополнений депозитов: 0

График:
- Серая линия (Переходы) поднялась выше
- Tooltip показывает "Переходы: 1"
```

#### 2.4 Проверь в БД:
```bash
sqlite3 /var/www/duo/site/server/data/database.db

SELECT user_id, referral_code, clicks FROM referral_stats;
# Должно показать: clicks = 1

SELECT referrer_id, referred_id FROM referrals;
# Должна быть запись о рефералле
```

✅ **ТЕСТ ПРОЙДЕН** если `clicks = 1` на сайте и в БД.

---

## 💰 ТЕСТ 3: ПЕРВЫЕ ДЕПОЗИТЫ {#тест-депозитов}

### Что проверяем:
Отслеживание первых депозитов рефералов.

### Шаги теста:

#### 3.1 Сделай депозит в боте:
```
1. В боте нажми "💳 Пополнить"
2. Введи сумму: 100
3. Оплати (тестовая карта Stripe: 4242 4242 4242 4242)
4. Дождись подтверждения
```

#### 3.2 Проверь статистику:
```
Обнови страницу (Ctrl+F5)

Ожидаемый результат:
- Переходы: 1
- Первые депозиты: 1 ✅
- Кол-во пополнений депозитов: 1 ✅
- Сумма депозитов: 100₽ ✅

График:
- Жёлтая линия (Первые депозиты) поднялась
- Зелёная линия (Депозиты) поднялась
- Tooltip показывает правильные значения
```

#### 3.3 Проверь в БД:
```bash
sqlite3 /var/www/duo/site/server/data/database.db

SELECT first_deposits, deposits_count, total_deposits FROM referral_stats;
# Должно показать:
# first_deposits = 1
# deposits_count = 1
# total_deposits = 100.0
```

✅ **ТЕСТ ПРОЙДЕН** если все значения совпадают.

---

## 🎲 ТЕСТ 4: ПРОИГРЫШИ И КОМИССИЯ {#тест-проигрышей}

### Что проверяем:
- Отслеживание проигрышей реферала
- Начисление 60% комиссии партнёру

### Шаги теста:

#### 4.1 Проиграй в игре:
```
1. В боте открой игру "Кости" (Roll)
2. Поставь 100₽
3. Проиграй (если выиграл - играй пока не проиграешь)
```

#### 4.2 Проверь статистику:
```
Обнови страницу (Ctrl+F5)

Ожидаемый результат:
- Средний доход с игрока: 60₽ ✅ (100₽ × 60%)

График:
- Красная линия (Доход) поднялась
- Tooltip показывает "Доход: 60₽"
```

#### 4.3 Проверь в БД:
```bash
sqlite3 /var/www/duo/site/server/data/database.db

SELECT total_earnings FROM referral_stats;
# Должно показать: 60.0 (100 × 0.6)
```

#### 4.4 Проиграй ещё раз:
```
Поставь 50₽ и проиграй

Ожидаемый результат на сайте:
- Средний доход с игрока: 90₽ ✅ (60₽ + 30₽)

В БД:
- total_earnings = 90.0 (60 + 50×0.6)
```

✅ **ТЕСТ ПРОЙДЕН** если комиссия = 60% от проигрыша.

---

## 💵 ТЕСТ 5: КОМИССИЯ 60% {#тест-комиссии}

### Что проверяем:
Правильность расчёта комиссии (должна быть 60%, не 10%).

### Таблица тестов:

| Проигрыш | Ожидаемая комиссия (60%) | Результат |
|----------|-------------------------|-----------|
| 10₽      | 6₽                      | ✅ / ❌    |
| 50₽      | 30₽                     | ✅ / ❌    |
| 100₽     | 60₽                     | ✅ / ❌    |
| 1000₽    | 600₽                    | ✅ / ❌    |

### Формула проверки:
```
Комиссия = Проигрыш × 0.6
```

### Проверка в коде:
```bash
grep -n "lossAmount \* 0.6" /var/www/duo/site/server/server.js
# Должна быть строка: const earnings = lossAmount * 0.6;
```

✅ **ТЕСТ ПРОЙДЕН** если все значения совпадают с таблицей.

---

## 🔄 ТЕСТ 6: НЕСКОЛЬКО РЕФЕРАЛОВ {#тест-рефералов}

### Что проверяем:
Суммирование статистики от нескольких рефералов.

### Шаги теста:

#### 6.1 Создай 2 реферала:
```
Реферал 1:
- Переход по ссылке
- Депозит: 100₽
- Проигрыш: 50₽

Реферал 2:
- Переход по ссылке
- Депозит: 200₽
- Проигрыш: 100₽
```

#### 6.2 Проверь суммарную статистику:
```
Ожидаемый результат:
- Переходы: 2 ✅
- Первые депозиты: 2 ✅
- Кол-во пополнений: 2 ✅
- Сумма депозитов: 300₽ ✅ (100 + 200)
- Доход: 90₽ ✅ (50×0.6 + 100×0.6 = 30 + 60)
```

✅ **ТЕСТ ПРОЙДЕН** если все значения суммируются правильно.

---

## 🧹 СБРОС СТАТИСТИКИ {#сброс-статистики}

### Для нового теста очисти БД:

```bash
ssh root@77.239.125.70

# Создай бэкап перед очисткой
cd /var/www/duo/site/server/data
cp database.db database_backup_$(date +%Y%m%d_%H%M%S).db

# Очисти статистику
sqlite3 database.db << EOF
DELETE FROM referral_stats;
DELETE FROM referrals;
EOF

# Перезапусти сервисы
pm2 restart all

# Проверь что очистилось
sqlite3 database.db "SELECT COUNT(*) FROM referral_stats;"
# Должно быть: 0
```

---

## ✅ ЧЕКЛИСТ УСПЕШНОГО ТЕСТА

### График:
- [ ] Все 4 линии видны и не накладываются
- [ ] Tooltip показывает реальные значения (не с offset)
- [ ] Точки видны на линиях
- [ ] При hover точка увеличивается

### Переходы:
- [ ] Клик по реферальной ссылке регистрируется
- [ ] Сообщение в боте упоминает 60%
- [ ] Статистика на сайте обновляется

### Депозиты:
- [ ] Первый депозит отслеживается
- [ ] Сумма депозитов считается правильно
- [ ] График обновляется

### Комиссия:
- [ ] Комиссия = 60% от проигрыша (не 10%!)
- [ ] Комиссия начисляется после каждого проигрыша
- [ ] Суммарная комиссия считается правильно

### Несколько рефералов:
- [ ] Статистика суммируется правильно
- [ ] Каждый реферал отслеживается отдельно
- [ ] Общий доход = сумма комиссий от всех

---

## 🐛 ЧАСТЫЕ ПРОБЛЕМЫ

### Проблема: График показывает 15 вместо 2
**Решение:** Обнови страницу с `Ctrl+Shift+F5`, проверь версию `chart.js?v=34`

### Проблема: Линии накладываются друг на друга
**Решение:** Обнови сервер (`git pull && pm2 restart duo-partner`)

### Проблема: Комиссия 10% вместо 60%
**Решение:** Проверь `site/server/server.js` строка 1041: `const earnings = lossAmount * 0.6;`

### Проблема: Статистика не обновляется
**Решение:** 
1. Проверь что все сервисы online (`pm2 list`)
2. Проверь логи (`pm2 logs duo-partner`)
3. Проверь что webhook работает (`pm2 logs duo-casino`)

### Проблема: Реферал не регистрируется
**Решение:**
1. Проверь что бот получает `start=ref_XXXXX`
2. Проверь логи бота: `pm2 logs duo-bot`
3. Проверь что API endpoint `/api/referral/register` работает

---

## 📝 ЗАМЕТКИ

- Все тесты проводить на **ТЕСТОВЫХ данных**
- Создавать **бэкапы БД** перед тестами
- **Документировать** все найденные баги
- **Проверять логи** при любых проблемах

**Готово для тестирования!** 🚀
