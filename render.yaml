services:
  - type: web
    name: telegram-games
    env: node
    rootDir: .
    buildCommand: cd server && npm install
    startCommand: node server/server.js
    envVars:
      - key: NODE_ENV
        value: production
