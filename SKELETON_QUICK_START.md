# ⚡ Skeleton Loader - Быстрый Старт

## 🚀 Автоматическая работа

**Скелетоны показываются АВТОМАТИЧЕСКИ** при загрузке данных с сервера!

---

## ✨ Что работает автоматически:

### 1. ☁️ Все fetch запросы к API

```javascript
// ПРОСТО делай fetch - скелетон покажется сам!
fetch('/api/referral/123')
  .then(response => response.json())
  .then(data => {
    // Отобразить данные
    displayData(data);
    // Скелетон скроется АВТОМАТИЧЕСКИ!
  });
```

### 2. 🖼️ Все изображения

```html
<!-- Lazy loading изображений -->
<img data-src="image.jpg" alt="Product" loading="lazy">

<!-- Скелетон покажется автоматически пока грузится! -->
```

### 3. 📦 Контейнеры для данных

Добавь один из классов к контейнеру:
- `.games-grid` - для сетки игр
- `.transactions-list` - для транзакций
- `.referral-list` - для рефералов  
- `.profile-section` - для профиля
- `.balance-display` - для баланса

**Скелетон появится автоматически при fetch!**

---

## 💻 Использование

### Вариант 1: Автоматический (РЕКОМЕНДУЕТСЯ)

```javascript
// Просто используй fetch к /api/* endpoints
fetch('/api/games')
  .then(r => r.json())
  .then(games => {
    displayGames(games); // Скелетон исчезнет сам
  });
```

### Вариант 2: С хелпером fetchWithSkeleton

```javascript
// Указываем контейнер явно
fetchWithSkeleton('/api/data', '.my-container')
  .then(data => {
    displayData(data);
  });
```

### Вариант 3: Ручное управление

```javascript
// Показать скелетон
showSkeleton('.my-container');

// Загрузить данные
loadData().then(() => {
  // Скрыть скелетон
  hideSkeleton('.my-container');
});
```

---

## 🎯 Примеры

### Загрузка игр

```javascript
// Контейнер с классом .games-grid
const container = document.querySelector('.games-grid');

// Fetch автоматически покажет скелетон
fetch('/api/games')
  .then(r => r.json())
  .then(games => {
    container.innerHTML = '';
    games.forEach(game => {
      container.appendChild(createGameCard(game));
    });
    // Скелетон исчезнет автоматически!
  });
```

### Загрузка рефералов

```javascript
// Контейнер .referral-list
fetch('/api/referral/user123')
  .then(r => r.json())
  .then(data => {
    displayReferrals(data.referrals);
    // Скелетон скроется сам!
  });
```

### Загрузка профиля

```javascript
// Контейнер .profile-section
fetch('/api/user/profile')
  .then(r => r.json())
  .then(user => {
    displayProfile(user);
    // Всё автоматически!
  });
```

---

## 🔧 Настройки

```javascript
// Изменить минимальное время показа (мс)
SkeletonLoader.config.minDisplayTime = 500;

// Отключить автоопределение (не рекомендуется)
SkeletonLoader.config.autoDetect = false;

// Отключить IntersectionObserver для изображений
SkeletonLoader.config.imageObserver = false;
```

---

## 📊 API Endpoints → Контейнеры

Автоматически определяются:

| API Endpoint | Контейнер |
|--------------|-----------|
| `/api/referral` | `.referral-list` |
| `/api/balance` | `.balance-display` |
| `/api/games` | `.games-grid` |
| `/api/transactions` | `.transactions-list` |
| `/api/user` | `.profile-section` |
| `/api/telegram-user` | `.user-info` |

---

## 🎨 Типы скелетонов

Автоматически выбираются по классу контейнера:

- `.games-grid` → Сетка карточек (6 шт)
- `.transactions-list` → Список (5 элементов)
- `.referral-list` → Список (5 элементов)
- `.profile-section` → Профиль
- `.balance-display` → Текст

---

## ⚡ Производительность

✅ **IntersectionObserver** - загрузка изображений только при скролле  
✅ **Минимальное время показа** - 300ms (убирает мерцание)  
✅ **Перехват fetch** - автоматическое определение загрузки  
✅ **MutationObserver** - отслеживание новых элементов

---

## 🐛 Troubleshooting

### Скелетон не появляется

1. Проверь класс контейнера:
```javascript
// Добавь класс
container.classList.add('games-grid');
```

2. Или используй явно:
```javascript
fetchWithSkeleton('/api/data', '.my-container');
```

### Скелетон не исчезает

```javascript
// Скрой вручную
hideSkeleton('.my-container');

// Или все
SkeletonLoader.hideAll();
```

---

## 📦 Готовые хелперы

```javascript
// Глобальные функции
showSkeleton('.container');     // Показать
hideSkeleton('.container');      // Скрыть
fetchWithSkeleton(url, '.cont'); // Fetch со скелетоном
```

---

## ✅ Checklist интеграции

1. ✅ Файлы подключены в `index.html`
2. ✅ Добавь класс контейнеру (`.games-grid`, `.transactions-list` и т.д.)
3. ✅ Используй `fetch('/api/...')` - всё остальное автоматически!

---

## 🎉 Всё готово!

**Больше ничего делать не нужно!**  
Скелетоны работают автоматически при каждом fetch запросе к `/api/*`.

---

## 💡 Tips

- Добавь `loading="lazy"` к `<img>` для автоскелетонов
- Используй классы `.games-grid`, `.transactions-list` для автоопределения
- Минимальное время показа 300ms убирает мерцание
- IntersectionObserver оптимизирует загрузку изображений

**Просто пиши код как обычно - скелетоны появятся сами!** 🚀
