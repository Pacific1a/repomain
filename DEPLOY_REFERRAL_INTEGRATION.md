# 🚀 ДЕПЛОЙ РЕФЕРАЛЬНОЙ ИНТЕГРАЦИИ

## 📋 ЧТО ДОБАВЛЕНО:

1. **ReferralService** - сервис для отправки данных в партнёрскую систему
2. **user_referrer** поле в БД - хранит код партнёра который пригласил
3. **Автоматическое сохранение** referrer_code при `/start ref_CODE`
4. **Автоматическая отправка депозитов** на партнёрский сервер
5. **Подготовка** для отправки проигрышей (60% партнёру)

---

## 🚀 КОМАНДЫ НА СЕРВЕРЕ:

```bash
# ШАГ 1: Обновить код
cd /var/www/duo
git pull origin main

# ШАГ 2: Обновить зависимости Node.js сервера
cd /var/www/duo/server
npm install
pm2 restart duo-server

# ШАГ 3: ВАЖНО! Добавить колонку user_referrer в БД
cd /var/www/duo/bot/autoshop
python3 << 'EOF'
import sqlite3
from tgbot.data.config import PATH_DATABASE

# Подключиться к БД
con = sqlite3.connect(PATH_DATABASE)
cursor = con.cursor()

# Проверить есть ли уже колонка
cursor.execute("PRAGMA table_info(storage_users)")
columns = [col[1] for col in cursor.fetchall()]

if 'user_referrer' not in columns:
    # Добавить колонку
    cursor.execute("ALTER TABLE storage_users ADD COLUMN user_referrer TEXT DEFAULT NULL")
    con.commit()
    print("✅ Колонка user_referrer добавлена!")
else:
    print("ℹ️ Колонка user_referrer уже существует")

con.close()
EOF

# ШАГ 4: Перезапустить Python бота
pm2 restart duo-bot

# ШАГ 5: Проверить логи
pm2 logs --lines 20
```

---

## 🧪 ТЕСТИРОВАНИЕ:

### 1. Проверить что всё запустилось:

```bash
pm2 status

# Должно быть:
# duo-server | online
# duo-bot    | online
```

### 2. Протестировать переход по ссылке:

В Telegram боте отправьте:
```
/start ref_3_MJPWJ47T8VU0O
```

Должно появиться:
```
🎁 Вы перешли по реферальной ссылке!
Ваш партнёр будет получать 60% от ваших проигрышей.
```

### 3. Протестировать пополнение:

Используйте тестовую команду:
```
/test_balance
```

Или реальное пополнение через бота.

### 4. Проверить что данные попали в партнёрку:

Откройте дашборд:
```
https://duopartners.xyz/dashboard/
```

**Статистика должна обновиться!**
- ✅ Clicks (переходы)
- ✅ Deposits (депозиты)
- ✅ График с данными

---

## 📊 ЧТО РАБОТАЕТ ПОСЛЕ ДЕПЛОЯ:

✅ Переходы по ссылке регистрируются  
✅ Депозиты отправляются партнёру  
⏳ Проигрыши пока не работают (нужна доп. синхронизация)

---

## 🔧 ЕСЛИ ПРОИГРЫШИ НЕ РАБОТАЮТ:

Проигрыши в играх требуют синхронизации `referrer_code` между:
- Python БД (где хранится user_referrer)
- Node.js сервер (где обрабатываются игры)

Создам дополнительный API endpoint для этого после тестирования депозитов!

---

**ВЫПОЛНИТЕ КОМАНДЫ И ПРОТЕСТИРУЙТЕ!** 🚀
