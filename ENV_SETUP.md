# 🔧 НАСТРОЙКА ПЕРЕМЕННЫХ ОКРУЖЕНИЯ

## ⚠️ КРИТИЧЕСКИ ВАЖНО!

Для работы реферальной системы необходимо установить **ОДИНАКОВЫЙ** `PARTNER_API_SECRET` на всех сервисах!

---

## 🤖 PYTHON БОТ (Автошоп)

### Локально (для разработки)

Создай файл `.env` в папке `bot/autoshop/`:

```env
# bot/autoshop/.env

SERVER_URL=https://duo-partner.onrender.com
PARTNER_API_SECRET=твой-секретный-ключ-123
```

### На Render (если бот деплоится)

1. Открой Python бот сервис на Render
2. Settings → Environment Variables
3. Добавь переменные:

```
SERVER_URL=https://duo-partner.onrender.com
PARTNER_API_SECRET=твой-секретный-ключ-123
```

### Проверка

После запуска бота проверь логи:

```
📡 Используется SERVER_URL: https://duo-partner.onrender.com
🔑 PARTNER_API_SECRET установлен: ✅
```

**Если видишь:**
```
🔑 PARTNER_API_SECRET установлен: ⚠️ ИСПОЛЬЗУЕТСЯ ДЕФОЛТНЫЙ КЛЮЧ!
```

**Значит переменная не установлена!** Установи через `.env` или Environment Variables.

---

## 👥 СЕРВЕР ПАРТНЁРОВ (duo-partner)

### На Render

1. Открой сервис **duo-partner**
2. Settings → Environment Variables
3. Убедись что есть:

```
PARTNER_API_SECRET=твой-секретный-ключ-123
```

**⚠️ ДОЛЖЕН БЫТЬ ТОЧНО ТАКОЙ ЖЕ КАК В БОТЕ!**

---

## 🎮 ИГРОВОЙ СЕРВЕР (telegram-games-plkj)

Если игровой сервер тоже отправляет webhook'и на duo-partner:

1. Открой сервис **telegram-games-plkj**
2. Settings → Environment Variables
3. Добавь:

```
PARTNER_SITE_URL=https://duo-partner.onrender.com
PARTNER_API_SECRET=твой-секретный-ключ-123
```

---

## 🔑 ГЕНЕРАЦИЯ БЕЗОПАСНОГО КЛЮЧА

Используй один из способов:

### Вариант 1: Онлайн генератор
```
https://www.random.org/strings/
Length: 32
Characters: a-z, A-Z, 0-9
```

### Вариант 2: Node.js
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```

### Вариант 3: Python
```bash
python -c "import secrets; print(secrets.token_urlsafe(32))"
```

### Вариант 4: PowerShell
```powershell
-join ((65..90) + (97..122) + (48..57) | Get-Random -Count 32 | % {[char]$_})
```

**Пример ключа:**
```
a7f8e2d9c4b5a6f3e8d1c2b7a9f5e3d8c6b4a2f7e9d5c3b8a6f4e2d7c9b5a3f8
```

---

## 📊 ПОЛНАЯ СХЕМА ПЕРЕМЕННЫХ

```
┌─────────────────────────────────────────────────┐
│  PYTHON БОТ (Автошоп)                           │
│  ├─ SERVER_URL=https://duo-partner.onrender.com │
│  └─ PARTNER_API_SECRET=secret-123               │
└─────────────────────────────────────────────────┘
                    ↓ POST + X-API-Secret
┌─────────────────────────────────────────────────┐
│  СЕРВЕР ПАРТНЁРОВ (duo-partner)                 │
│  └─ PARTNER_API_SECRET=secret-123 ← ТАКОЙ ЖЕ!   │
└─────────────────────────────────────────────────┘
                    ↓ Webhook успешно
┌─────────────────────────────────────────────────┐
│  ИГРОВОЙ СЕРВЕР (telegram-games-plkj)           │
│  ├─ PARTNER_SITE_URL=https://duo-partner...     │
│  └─ PARTNER_API_SECRET=secret-123 ← ТАКОЙ ЖЕ!   │
└─────────────────────────────────────────────────┘
```

---

## ✅ ПРОВЕРКА НАСТРОЕК

### 1. Проверь логи Python бота

```
📡 Используется SERVER_URL: https://duo-partner.onrender.com
🔑 PARTNER_API_SECRET установлен: ✅
```

### 2. Проверь Environment Variables на Render

**duo-partner:**
```
PARTNER_API_SECRET: ••••••••••••••••••••••••
```

**telegram-games-plkj:**
```
PARTNER_API_SECRET: ••••••••••••••••••••••••
PARTNER_SITE_URL: https://duo-partner.onrender.com
```

### 3. Тест webhook

Открой в Telegram боте: `/start ref_test123`

**Логи Python бота:**
```
🔍 Referral link detected: full_code=ref_test123, extracted_user_id=test, new_user=YOUR_ID
📡 Server response status: 200
✅ Referral registered: YOUR_ID -> test
```

**Логи duo-partner:**
```
✅ Webhook authenticated
📥 Referral registration request: userId=YOUR_ID, referrerId=test
✅ Referral registered: YOUR_ID → partner X, clicks=1
```

---

## ⚠️ ЧАСТЫЕ ОШИБКИ

### Ошибка: 401 Unauthorized

**Логи бота:**
```
📡 Server response status: 401
{"success":false,"message":"Unauthorized"}
```

**Причина:** `PARTNER_API_SECRET` не совпадают или не установлены

**Решение:**
1. Проверь Environment Variables на duo-partner
2. Проверь `.env` или Environment Variables бота
3. Убедись что значения **ТОЧНО ОДИНАКОВЫЕ**
4. Передеплой оба сервиса

---

### Ошибка: "ИСПОЛЬЗУЕТСЯ ДЕФОЛТНЫЙ КЛЮЧ"

**Логи бота:**
```
🔑 PARTNER_API_SECRET установлен: ⚠️ ИСПОЛЬЗУЕТСЯ ДЕФОЛТНЫЙ КЛЮЧ!
```

**Причина:** Переменная `PARTNER_API_SECRET` не установлена

**Решение:**
1. Создай `.env` файл в `bot/autoshop/`
2. Добавь: `PARTNER_API_SECRET=твой-ключ`
3. Или установи через Environment Variables на Render
4. Перезапусти бота

---

### Ошибка: Timeout / Connection refused

**Логи бота:**
```
❌ Timeout registering referral - server did not respond
```

**Причина:** Неправильный `SERVER_URL` или сервер не работает

**Решение:**
1. Проверь что `SERVER_URL=https://duo-partner.onrender.com`
2. Открой в браузере https://duo-partner.onrender.com
3. Должен открыться сайт партнёров
4. Проверь логи duo-partner на Render

---

## 📝 CHECKLIST

### Перед запуском:

- [ ] Сгенерирован безопасный `PARTNER_API_SECRET`
- [ ] Установлен в Python боте (`.env` или ENV)
- [ ] Установлен на duo-partner (ENV)
- [ ] Установлен на telegram-games-plkj (ENV, если нужно)
- [ ] Все значения **ОДИНАКОВЫЕ**
- [ ] Бот перезапущен (если локально)
- [ ] Сервисы передеплоены (если на Render)
- [ ] Проверены логи всех сервисов
- [ ] Выполнен тест `/start ref_test`

---

## 🎯 ИТОГ

После правильной настройки:

✅ Python бот отправляет запросы с заголовком `X-API-Secret`  
✅ duo-partner проверяет и принимает запросы  
✅ Статус 200, success: true  
✅ Реферал регистрируется  
✅ Статистика обновляется  

**ВСЁ РАБОТАЕТ!** 🚀
