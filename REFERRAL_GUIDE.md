# 🎁 Руководство по реферальной системе

## ✅ Что уже реализовано:

### 1. **Файлы системы**
- `referral-system.js` - основная система рефералов (клиент)
- `referral-api.js` - API эндпоинты для сервера
- `referral-integration.js` - автоматическое начисление процентов при выигрышах

### 2. **Функционал**
- ✅ Генерация реферальных ссылок
- ✅ Копирование ссылки в буфер обмена
- ✅ Регистрация рефералов при переходе по ссылке
- ✅ Автоматическое начисление 10% от выигрышей партнеру
- ✅ Вывод средств с реферального баланса с комиссией 5%
- ✅ Отображение списка приглашенных друзей
- ✅ Статистика по каждому рефералу (выигрыши, прибыль)

## 📦 Установка на сервер:

### Шаг 1: Добавьте код в server.js

Откройте `server/referral-api.js` и скопируйте весь код в `server.js` **ПЕРЕД** строкой:
```javascript
// ============ ROLL BOTS AUTO-SPAWN ============
```

### Шаг 2: Создайте папку для данных

Убедитесь что существует папка `server/data/`:
```bash
mkdir server/data
```

### Шаг 3: Перезапустите сервер
```bash
cd server
npm start
```

## 🎮 Как работает система:

### 1. **Генерация реферальной ссылки**

Когда пользователь нажимает кнопку "Invite a friend":
```javascript
// Генерируется ссылка вида:
https://t.me/YOUR_BOT?start=ref1889923046

// Где 1889923046 - это Telegram ID пригласившего
```

### 2. **Регистрация реферала**

Когда новый пользователь переходит по ссылке:
```javascript
// URL содержит параметр ?ref=1889923046
// Система автоматически регистрирует связь:
POST /api/referral/register
{
  "userId": "новый_id",
  "referrerId": "1889923046"
}
```

### 3. **Начисление процентов**

Когда реферал выигрывает в играх:
```javascript
// При вызове window.BalanceAPI.addChips(1000, 'upgrade', 'Win')
// Автоматически начисляется 10% партнеру:

Выигрыш реферала: 1000 фишек
Начисление партнеру: 100 фишек (10%)
```

Поддерживаемые игры:
- upgrade
- crash
- roll
- mines
- blackjack
- speedcash

### 4. **Вывод средств**

Когда партнер выводит средства:
```
Реферальный баланс: 1000₽
Комиссия 5%: 50₽
На основной баланс: 950₽
```

## 🎨 UI компоненты:

### Кнопки:
- `.invite-button` - генерирует ссылку
- `.copy-button` - копирует ссылку
- `.withdraw-button` - выводит средства

### Отображение данных:
- `.text-wrapper-4` - текст ссылки (в кнопке invite)
- `.text-wrapper-7` - реферальный баланс
- `.text-wrapper-9` (в `.invidet-amount`) - количество рефералов

### Карточка реферала (`.refferal-info-2`):
- `.avatar-2` - аватар
- `.text-wrapper-13` - ник
- `.text-wrapper-14` - сумма выигрыша
- `.text-wrapper-15` - ваша прибыль

## 📝 API Эндпоинты:

### GET /api/referral/:telegramId
Получить данные о рефералах пользователя

**Response:**
```json
{
  "referralCode": "1889923046",
  "referralBalance": 150.50,
  "totalEarnings": 250.00,
  "referrals": [
    {
      "userId": "123456789",
      "nickname": "User1234",
      "registeredAt": 1701234567890,
      "totalWinnings": 5000,
      "totalEarnings": 500
    }
  ]
}
```

### POST /api/referral/register
Зарегистрировать реферала

**Request:**
```json
{
  "userId": "123456789",
  "referrerId": "1889923046"
}
```

### POST /api/referral/add-earnings
Начислить процент рефереру (вызывается автоматически)

**Request:**
```json
{
  "userId": "123456789",
  "amount": 1000
}
```

**Response:**
```json
{
  "success": true,
  "referrerId": "1889923046",
  "commission": 100,
  "referralBalance": 250.50
}
```

### POST /api/referral/withdraw
Вывести средства с реферального баланса

**Request:**
```json
{
  "userId": "1889923046",
  "amount": 1000
}
```

**Response:**
```json
{
  "success": true,
  "withdrawn": 1000,
  "commission": 50,
  "received": 950,
  "newReferralBalance": 0,
  "newMainBalance": 1950
}
```

## 🔧 Интеграция с играми:

### Автоматическая интеграция (уже работает):

Если игра использует `window.BalanceAPI.addChips()`, проценты начисляются автоматически:

```javascript
// В любой игре:
await window.BalanceAPI.addChips(1000, 'upgrade', 'Win x2');

// Автоматически:
// 1. Добавляет 1000 фишек игроку
// 2. Проверяет, есть ли у него реферер
// 3. Начисляет 100 фишек (10%) рефереру
```

### Ручная интеграция:

Если нужно начислить проценты вручную:

```javascript
// В любом месте кода
await window.addReferralBonus(userId, winAmount);

// Или через систему напрямую:
await window.ReferralSystem.addReferralEarnings(userId, winAmount);
```

## 🎯 Настройка для вашего бота:

### 1. Измените имя бота

В `referral-system.js` найдите строку:
```javascript
const botUsername = 'your_bot'; // ЗАМЕНИТЕ НА ИМЯ ВАШЕГО БОТА
```

Измените на имя вашего бота:
```javascript
const botUsername = 'my_casino_bot';
```

### 2. Настройте обработку start параметров в боте

В коде бота (Python) добавьте обработку:

```python
@dp.message(CommandStart())
async def start_command(message: Message):
    args = message.text.split()[1:]  # Получаем параметры
    
    if args and args[0].startswith('ref'):
        referrer_id = args[0][3:]  # Убираем 'ref' префикс
        # Отправляем на сервер
        async with aiohttp.ClientSession() as session:
            await session.post(
                f"{SERVER_URL}/api/referral/register",
                json={
                    "userId": str(message.from_user.id),
                    "referrerId": referrer_id
                }
            )
```

## 📊 Структура данных:

### server/data/referrals.json:
```json
{
  "1889923046": {
    "referralCode": "1889923046",
    "referralBalance": 150.50,
    "totalEarnings": 250.00,
    "referrals": [
      {
        "userId": "123456789",
        "registeredAt": 1701234567890,
        "totalWinnings": 5000,
        "totalEarnings": 500
      }
    ]
  }
}
```

## 🚀 Подключение к другим страницам:

Чтобы проценты начислялись на всех страницах с играми, добавьте в HTML **ПОСЛЕ balance-api.js**:

```html
<script src="../balance-api.js"></script>
<script src="../referral-integration.js"></script>
```

Пример для upgrade/index.html:
```html
<script src="../balance-api.js"></script>
<script src="../referral-integration.js"></script> <!-- Добавить эту строку -->
<script src="index.js"></script>
```

## 🔐 Безопасность:

1. **Проверка на самореферал:**
```javascript
if (userId === referrerId) {
  return res.status(400).json({ error: 'Cannot refer yourself' });
}
```

2. **Проверка на дубликаты:**
```javascript
const alreadyReferred = referrals[referrerId].referrals.some(
  ref => ref.userId === userId
);
```

3. **Проверка баланса при выводе:**
```javascript
if (userRef.referralBalance < amount) {
  return res.status(400).json({ error: 'Insufficient balance' });
}
```

## 📱 WebSocket уведомления:

Система отправляет real-time уведомления:

```javascript
// При начислении процента
io.emit(`referral_earnings_${referrerId}`, {
  userId: userId,
  amount: commission,
  totalBalance: refData.referralBalance
});

// При выводе средств
io.emit(`balance_updated_${userId}`, {
  rubles: balances[userId].rubles,
  chips: balances[userId].chips
});
```

## 🐛 Отладка:

Включите логирование в консоли:
```javascript
// В браузере
console.log('ReferralSystem:', window.ReferralSystem);
console.log('Balance:', window.ReferralSystem.referralBalance);
console.log('Referrals:', window.ReferralSystem.referrals);
```

На сервере логи автоматические:
```
✅ User 123456789 registered by referrer 1889923046
💰 Added 100₽ to referrer 1889923046 from 123456789's win 1000₽
💸 Withdrawal: 1000₽ from referral balance, 950₽ to main (commission: 50₽)
```

## ✅ Чеклист запуска:

- [ ] Код из referral-api.js добавлен в server.js
- [ ] Папка server/data создана
- [ ] Сервер перезапущен
- [ ] Имя бота изменено в referral-system.js
- [ ] Обработка start параметров добавлена в бота
- [ ] referral-integration.js подключен на страницах с играми
- [ ] Скрипты подключены в refferall/index.html

## 🎉 Готово!

Система полностью готова к работе. Пользователи могут:
1. Создавать реферальные ссылки
2. Приглашать друзей
3. Получать 10% от их выигрышей
4. Выводить средства на основной баланс
