# 💰 Баланс не обновляется на сайте

## Причина:
Баланс хранится в `localStorage` браузера и обновляется только при логине.

---

## ✅ РЕШЕНИЕ:

### Вариант 1: Выйти и войти заново (БЫСТРО)

1. Нажми "Выйти" на сайте
2. Войди заново с email/паролем
3. Баланс обновится автоматически

---

### Вариант 2: Обновить localStorage вручную

Открой консоль браузера (F12) и выполни:

```javascript
// Получить текущие данные
let user = JSON.parse(localStorage.getItem('user'));

// Обновить баланс
user.balance = 99999;

// Сохранить обратно
localStorage.setItem('user', JSON.stringify(user));

// Обновить страницу
location.reload();
```

---

### Вариант 3: Проверить что SQL запрос выполнился

На сервере:

```bash
sqlite3 /var/www/duo/server/data/database.db "SELECT email, login, balance FROM users WHERE login = 'kuratormir07';"
```

Должно быть:
```
kuratormir07@gmail.com|kuratormir07|99999.0
```

Если баланс НЕ 99999 - значит SQL запрос не выполнился.

Выполни снова:
```bash
sqlite3 /var/www/duo/server/data/database.db "UPDATE users SET balance = 99999 WHERE login = 'kuratormir07';"
```

---

## 🔧 Дополнительно: Проверка через API

```bash
# Получи токен из localStorage браузера
TOKEN="твой_jwt_token"

# Проверь баланс через API
curl -H "Authorization: Bearer $TOKEN" https://duopartners.xyz/api/auth/user
```

Ответ должен содержать:
```json
{
  "success": true,
  "user": {
    "balance": 99999,
    ...
  }
}
```

---

## 📝 Итог:

**Самый быстрый способ** - просто выйти и войти заново! 🚀
