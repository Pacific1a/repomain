# 🖥️ ВАРИАНТ С ОТДЕЛЬНЫМ VPS СЕРВЕРОМ

## 🎯 ОТЛИЧНАЯ ИДЕЯ!

**С VPS сервером у тебя будет:**
- ✅ Полный контроль
- ✅ Persistent storage по умолчанию
- ✅ Можно использовать SQLite (код уже готов!)
- ✅ Нет ограничений бесплатного tier
- ✅ Быстрее чем Render
- ✅ Может быть дешевле

---

## 💰 ДЕШЕВЫЕ VPS ВАРИАНТЫ

### 🔥 Рекомендую (от дешевых к дорогим):

| Провайдер | Цена | RAM | Диск | Регион |
|-----------|------|-----|------|--------|
| **Timeweb** | ₽169/мес (~$2) | 1GB | 10GB SSD | RU |
| **Reg.ru** | ₽199/мес (~$2.5) | 512MB | 10GB SSD | RU |
| **Vultr** | $2.50/мес | 512MB | 10GB SSD | Global |
| **DigitalOcean** | $4/мес | 512MB | 10GB SSD | Global |
| **Hetzner** | €4.15/мес (~$4.5) | 2GB | 20GB SSD | EU |
| **Contabo** | €4/мес (~$4.5) | 4GB | 50GB SSD | EU |

### 🇷🇺 Российские (самые дешевые):

**Timeweb:**
- https://timeweb.com/ru/services/vds
- От ₽169/мес
- 1GB RAM, 10GB SSD
- Панель управления на русском
- Поддержка 24/7

**Reg.ru:**
- https://www.reg.ru/vps/
- От ₽199/мес
- 512MB RAM, 10GB SSD
- Легкая настройка

**Beget:**
- https://beget.com/ru/vps
- От ₽300/мес
- 1GB RAM, 25GB SSD
- Хорошая поддержка

### 🌍 Международные:

**Vultr:**
- https://www.vultr.com/
- От $2.50/мес
- Много локаций
- Быстрая сеть

**DigitalOcean:**
- https://www.digitalocean.com/
- От $4/мес (часто дают $200 кредитов)
- Отличная документация
- Популярный выбор

**Hetzner:**
- https://www.hetzner.com/cloud
- От €4.15/мес
- Германия (быстро для Европы)
- Отличная цена/качество

---

## 🚀 ЧТО НУЖНО СДЕЛАТЬ

### Шаг 1: Выбрать VPS

**Мой совет для начала:**

**Если в России → Timeweb (₽169/мес)**  
**Если нужен международный → Vultr ($2.50/мес)**

### Шаг 2: Что установить на сервере

```bash
# Ubuntu 22.04 LTS (рекомендую)

# 1. Обновить систему
sudo apt update && sudo apt upgrade -y

# 2. Установить Node.js 18+
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt install -y nodejs

# 3. Установить nginx (для проксирования)
sudo apt install -y nginx

# 4. Установить certbot (для SSL)
sudo apt install -y certbot python3-certbot-nginx

# 5. Установить git
sudo apt install -y git

# 6. Установить PM2 (для автозапуска)
sudo npm install -g pm2
```

### Шаг 3: Деплой приложения

```bash
# 1. Клонировать репозиторий
cd /var/www
sudo git clone https://github.com/Pacific1a/repomain.git duo

# 2. Установить зависимости
cd duo/site/server
sudo npm install

# 3. Создать .env файл
sudo nano .env
```

**Содержимое .env:**
```env
PORT=3000
JWT_SECRET=your-super-secret-key-change-me
DATABASE_PATH=/var/www/duo/site/server/data/database.db
PARTNER_API_SECRET=e1e6547a80623ab936abfe561a8a0871
```

```bash
# 4. Создать админа
sudo node create-admin.js

# 5. Запустить с PM2
sudo pm2 start server.js --name duo-partner
sudo pm2 startup
sudo pm2 save
```

### Шаг 4: Настроить nginx

```bash
sudo nano /etc/nginx/sites-available/duo-partner
```

**Содержимое:**
```nginx
server {
    listen 80;
    server_name your-domain.com;  # Измени на свой домен

    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
```

```bash
# Активировать конфиг
sudo ln -s /etc/nginx/sites-available/duo-partner /etc/nginx/sites-enabled/
sudo nginx -t
sudo systemctl reload nginx
```

### Шаг 5: Настроить SSL (HTTPS)

```bash
# Получить бесплатный SSL от Let's Encrypt
sudo certbot --nginx -d your-domain.com

# Certbot автоматически настроит HTTPS
```

### Шаг 6: Обновить Python бота

**В `bot/autoshop/tgbot/data/config.py`:**
```python
SERVER_URL = "https://your-domain.com"  # Твой домен
```

### Шаг 7: Настроить firewall

```bash
# Разрешить нужные порты
sudo ufw allow 22    # SSH
sudo ufw allow 80    # HTTP
sudo ufw allow 443   # HTTPS
sudo ufw enable
```

---

## ✅ ПРЕИМУЩЕСТВА VPS

### Почему это может быть лучше:

1. **SQLite работает из коробки** ✅
   - Код уже готов
   - Не нужна миграция
   - Persistent storage по умолчанию

2. **Дешевле чем Render Disk** ✅
   - Timeweb: ₽169/мес (~$2)
   - Render Disk: $7/мес
   - Экономия: $5/мес

3. **Полный контроль** ✅
   - Можешь установить что угодно
   - Логи доступны всегда
   - SSH доступ

4. **Нет ограничений** ✅
   - Нет лимитов запросов
   - Нет таймаутов idle
   - Нет sleep режима

5. **Быстрее** ✅
   - Выделенные ресурсы
   - Нет cold start

---

## ⚠️ НЕДОСТАТКИ VPS

1. **Нужно администрировать** ⚠️
   - Обновления безопасности
   - Мониторинг
   - Бэкапы

2. **Нужен домен** ⚠️
   - Купить домен (~$10/год)
   - Или использовать IP (не рекомендую)

3. **Ответственность за безопасность** ⚠️
   - Настроить firewall
   - Обновлять систему
   - Защита от атак

---

## 🎯 МОЯ РЕКОМЕНДАЦИЯ

### Если умеешь работать с Linux:
→ **VPS (Timeweb ₽169/мес)** - лучший вариант!

### Если НЕ умеешь работать с Linux:
→ **PostgreSQL на Render (бесплатно)** - я помогу мигрировать

### Если хочешь быстро и просто:
→ **Render Disk ($7/мес)** - код готов, работает сразу

---

## 💡 КОМБИНИРОВАННЫЙ ВАРИАНТ

**Можно использовать:**

1. **VPS для API** (site/server) ← основное приложение
2. **Render для бота** (bot/) ← бесплатный tier
3. **SQLite на VPS** ← persistent storage

**Связь:**
```
Python Bot (Render Free) 
    ↓ webhook
API Server (VPS + SQLite)
    ↓ статистика
Partner Dashboard
```

**Преимущества:**
- ✅ Бот на бесплатном Render
- ✅ API на быстром VPS
- ✅ База persistent
- ✅ Дешево

---

## 🛠️ Я МОГУ ПОМОЧЬ

### Если выберешь VPS, я помогу:

1. ✅ Выбрать подходящий VPS
2. ✅ Написать полные инструкции по настройке
3. ✅ Создать скрипт автоматической установки
4. ✅ Настроить nginx + SSL
5. ✅ Настроить PM2 для автозапуска
6. ✅ Настроить автоматические бэкапы
7. ✅ Проверить безопасность

---

## 📝 СРАВНЕНИЕ ВСЕХ ВАРИАНТОВ

| Вариант | Цена | Сложность | Время | Persistent | Код готов |
|---------|------|-----------|-------|------------|-----------|
| **VPS (Timeweb)** | ₽169/мес | Средняя | 1 час | ✅ YES | ✅ YES |
| **PostgreSQL (Render)** | Бесплатно | Средняя | 2 часа | ✅ YES | ❌ Нужна миграция |
| **Render Disk** | $7/мес | Легкая | 5 мин | ✅ YES | ✅ YES |
| **Supabase** | Бесплатно | Средняя | 2 часа | ✅ YES | ❌ Нужна миграция |

---

## 🚀 ЧТО ДЕЛАТЬ СЕЙЧАС?

### Скажи мне:

**1.** "У меня уже есть VPS" → я дам инструкции  
**2.** "Хочу арендовать VPS (Timeweb/другой)" → я помогу настроить  
**3.** "Не хочу возиться с VPS" → мигрируем на PostgreSQL (бесплатно)  
**4.** "Оплачу Render Disk" → следуй RENDER_SETUP.md  

---

## 📊 МОЙ ЛИЧНЫЙ ВЫБОР

**Если бы это был мой проект:**

**→ VPS (Timeweb ₽169/мес) + SQLite**

**Почему:**
- ✅ Дешевле всего в долгосрочной перспективе
- ✅ Код уже готов (SQLite)
- ✅ Полный контроль
- ✅ Быстрая работа
- ✅ Можно расширять (добавить Redis, еще сервисы)

**Минус:**
- ⚠️ Нужно один раз потратить 1 час на настройку

---

## 🎓 ДОПОЛНИТЕЛЬНО

### Если выберешь VPS, я создам:

1. **Автоматический скрипт установки** (один клик)
2. **Dockerfile** (если хочешь Docker)
3. **Инструкцию по бэкапам**
4. **Мониторинг через PM2**
5. **Автообновление через webhook**

---

**ЖДУ ТВОЕГО РЕШЕНИЯ!** 🚀

**У тебя есть VPS или будешь арендовать?**
