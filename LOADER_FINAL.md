# ✅ LOADER - ФИНАЛЬНАЯ КОНФИГУРАЦИЯ

## 🎯 ТРЕБОВАНИЯ ИЗ промт.md

✅ Показывается **минимум 10 секунд** при первом открытии приложения  
✅ **НЕ показывается** при повторных переходах на главную страницу в рамках одной сессии  
✅ Сбрасывается только при **полном закрытии/перезагрузке** приложения  

---

## ⚙️ КОНФИГУРАЦИЯ

### **bot/preloader.js:**
```javascript
const STORAGE_KEY = 'initialLoadComplete';        // Общий ключ для всей сессии
const SPINNER_MIN_DURATION = 10000;               // 10 секунд минимум
```

### **Логика работы:**
```javascript
// 1. Проверка - была ли первая загрузка
if (wasInitialLoadComplete()) {
  hideSpinner();  // Скрываем сразу
  return;
}

// 2. Первая загрузка
const startTime = Date.now();
showSpinner();

// Загружаем 2 критичных изображения
await preloadImages(images);

// 3. Гарантируем минимум 10 секунд показа
const elapsed = Date.now() - startTime;
if (elapsed < 10000) {
  await new Promise(resolve => 
    setTimeout(resolve, 10000 - elapsed)
  );
}

// 4. Скрываем и сохраняем флаг
hideSpinner();
sessionStorage.setItem('initialLoadComplete', 'true');
```

---

## 📊 ПРИМЕРЫ РАБОТЫ

### **Сценарий 1: Быстрая загрузка**
```
Время загрузки изображений: 2 секунды
Оставшееся время: 10s - 2s = 8 секунд
Итого loader показан: 10 секунд
```

### **Сценарий 2: Медленная загрузка**
```
Время загрузки изображений: 12 секунд
Оставшееся время: 0 секунд (уже больше 10s)
Итого loader показан: 12 секунд
```

### **Сценарий 3: Навигация**
```
1. Первое открытие → Loader 10+ секунд
2. Переход на upgrade → Loader НЕТ
3. Возврат на main → Loader НЕТ
4. Переход на games → Loader НЕТ
```

### **Сценарий 4: Повторное открытие**
```
1. Закрыл мини-апп (свайп вниз в Telegram)
2. sessionStorage['initialLoadComplete'] очищен
3. Открыл @TwinUpBot снова
4. Loader показывается 10+ секунд опять
```

---

## 🧪 ТЕСТИРОВАНИЕ

### **Проверка времени:**
```javascript
// В консоли браузера
const start = Date.now();
// После исчезновения loader:
console.log('Loader показан:', Date.now() - start, 'ms');
// Должно быть >= 10000ms
```

### **Проверка sessionStorage:**
```javascript
// После первой загрузки
sessionStorage.getItem('initialLoadComplete')
// Ожидается: "true"

// Для теста - сбросить
sessionStorage.removeItem('initialLoadComplete')
location.reload()
// Loader должен показаться снова
```

### **Проверка навигации:**
```
1. Открой main → Loader 10s
2. Перейди upgrade → Loader НЕТ
3. Вернись main → Loader НЕТ
4. Открой games → Loader НЕТ
```

---

## 📝 CONSOLE ЛОГИ

### **Первая загрузка:**
```
🎬 Первая загрузка мини-аппа, показываем loader...
🖼️ Загружаю 2 изображений...
✅ Загружено: 2/2
🎉 Первая загрузка завершена! При навигации loader не будет показываться.
📝 Первая загрузка завершена, loader не будет показываться в этой сессии
```

### **Повторные визиты:**
```
✅ Первая загрузка уже выполнена в этой сессии, loader не показываем
```

---

## 🚀 ДЕПЛОЙ

```bash
cd /var/www/duo
git pull origin main
```

**Проверить коммиты:**
```bash
git log --oneline -3
# 0daa7e9 feat: Increase loader minimum duration to 10 seconds
# 6266a1d docs: Add loader session guide
# 8ac7340 feat: Loader shows once per Telegram Mini App session
```

---

## ✅ ИТОГОВОЕ СОСТОЯНИЕ

| Параметр | Значение |
|----------|----------|
| **Минимальное время показа** | 10 секунд |
| **Показывается при первом открытии** | ✅ Да |
| **Показывается при навигации** | ❌ Нет |
| **Показывается после закрытия мини-аппа** | ✅ Да |
| **sessionStorage ключ** | `'initialLoadComplete'` |
| **Загружаемых изображений** | 2 (Group 8.png, Group 9.png) |
| **Timeout на изображение** | 5 секунд |

---

## 🎯 СООТВЕТСТВИЕ ТРЕБОВАНИЯМ

**Из промт.md:**

1. ✅ **Показывается минимум 10 секунд при первом открытии**
   - `SPINNER_MIN_DURATION = 10000`
   - Гарантирует минимум 10s независимо от скорости загрузки

2. ✅ **НЕ показывается при повторных переходах в рамках сессии**
   - Использует `sessionStorage['initialLoadComplete']`
   - Проверяется при каждой загрузке страницы

3. ✅ **Сбрасывается только при полном закрытии/перезагрузке**
   - sessionStorage очищается автоматически при закрытии Telegram WebView
   - При повторном открытии мини-аппа loader показывается снова

---

## 💡 ВАЖНО

**Почему НЕ используем App.js подход из промт.md:**
- Приложение на **чистом HTML/JS** (не React SPA)
- Каждая страница - отдельный HTML файл
- Нет корневого компонента App.js
- Используем **sessionStorage** вместо React state

**Почему текущее решение работает:**
- sessionStorage живёт пока открыт Telegram WebView
- Ключ 'initialLoadComplete' общий для всех страниц
- При навигации между страницами sessionStorage сохраняется
- При закрытии мини-аппа sessionStorage очищается

---

Loader теперь работает **точно по требованиям** из промт.md! 🎉
