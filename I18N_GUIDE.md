# 🌐 Руководство по мультиязычности (i18n)

## ✅ Что уже сделано:

### 1. **Создана система локализации**
- `i18n.js` - основная система переводов (русский + английский)
- `language-switcher.js` - красивый переключатель языка с флагами 🇷🇺🇬🇧
- `i18n-adaptive.css` - адаптивные стили для предотвращения поломки верстки

### 2. **Локализован профиль**
- Все тексты переведены
- Добавлен переключатель языка (правый верхний угол)
- Верстка адаптирована под разную длину текста

## 📦 Как подключить к другим страницам:

### Шаг 1: Подключите CSS
```html
<head>
  ...
  <link rel="stylesheet" href="../i18n-adaptive.css" />
</head>
```

### Шаг 2: Подключите скрипты
```html
<body>
  ...
  <!-- ВАЖНО: i18n.js должен быть ПЕРВЫМ -->
  <script src="../i18n.js"></script>
  <script src="../language-switcher.js"></script>
  <script src="../balance-api.js"></script>
  ...
</body>
```

### Шаг 3: Добавьте data-i18n атрибуты

**ДО:**
```html
<div class="text-wrapper">Balance</div>
<button class="deposit-button">Deposit</button>
```

**ПОСЛЕ:**
```html
<div class="text-wrapper" data-i18n="balance">Balance</div>
<button class="deposit-button" data-i18n="deposit">Deposit</button>
```

## 🎯 Использование в коде:

### JavaScript:
```javascript
// Получить перевод
const text = window.i18n.t('balance'); // "Баланс" или "Balance"

// Сменить язык программно
window.i18n.setLanguage('en');

// Получить текущий язык
const lang = window.i18n.getCurrentLanguage(); // "ru" или "en"

// Слушать смену языка
window.addEventListener('languageChanged', (e) => {
  console.log('Язык изменён на:', e.detail.lang);
  // Обновите динамический контент
});
```

### Динамический контент:
```javascript
// Для динамически создаваемых элементов
function createTransaction(amount) {
  const div = document.createElement('div');
  div.innerHTML = `
    <span data-i18n="transaction_add">Пополнение</span>: ${amount}₽
  `;
  
  // После вставки в DOM - обновить переводы
  window.i18n.updatePage();
  
  return div;
}
```

## 📝 Добавление новых переводов:

Откройте `i18n.js` и добавьте ключи в оба языка:

```javascript
const translations = {
  ru: {
    ...
    'my_new_key': 'Мой новый текст',
  },
  en: {
    ...
    'my_new_key': 'My new text',
  }
};
```

Затем используйте в HTML:
```html
<div data-i18n="my_new_key">Мой новый текст</div>
```

## 🎨 Переключатель языка

Переключатель автоматически создаётся на всех страницах где подключен `language-switcher.js`:

- Расположение: правый верхний угол
- Дизайн: флаги 🇷🇺 🇬🇧 с градиентной подсветкой активного языка
- Сохранение: выбор сохраняется в localStorage
- Адаптивность: уменьшается на мобильных

## 🛡️ Предотвращение поломки верстки:

### Проблема:
Английские слова длиннее русских → может сломаться дизайн

### Решение в `i18n-adaptive.css`:

1. **Автоматическое усечение длинных текстов:**
```css
[data-i18n] {
  white-space: nowrap;
  overflow: hidden;
  text-overflow: ellipsis;
}
```

2. **Адаптивные размеры для меню:**
```css
html[lang="en"] .menu a [data-i18n] {
  font-size: 11px; /* Меньше шрифт для английского */
}
```

3. **Растягивание кнопок под текст:**
```css
.deposit-button {
  min-width: fit-content;
  padding: 0 16px;
}
```

## 📋 Список доступных ключей:

### Общие:
- `loading` - Загрузка... / Loading...
- `error` - Ошибка / Error
- `balance` - Баланс / Balance
- `deposit` - Пополнить / Deposit

### Меню:
- `main` - Главная / Main
- `upgrade` - Апгрейд / Upgrade
- `game` - Игры / Games
- `swap` - Обмен / Swap
- `profile` - Профиль / Profile

### Upgrade:
- `bet_amount` - Сумма ставки / Bet Amount
- `desired_prize` - Желаемый выигрыш / Desired prize
- `chance` - Шанс / Chance
- `apply` - Применить / Apply
- `upgrade_button` - Апгрейд / Upgrade
- `min_bet` - Минимальная ставка / Min bet
- `max_bet` - Максимальная ставка / Max bet
- `no_chips` - У вас нет фишек! / You have no chips!
- `insufficient_chips` - Недостаточно фишек / Insufficient chips
- `bet_accepted` - Ставка принята! / Bet accepted!

### Транзакции:
- `your_transactions` - Ваши транзакции / Your transactions
- `show_more` - Показать ещё / Show more
- `transaction_add` - Пополнение / Deposit
- `transaction_subtract` - Списание / Withdrawal
- `transaction_win` - Выигрыш / Win
- `transaction_bet` - Ставка / Bet

### Swap:
- `exchange` - Обмен / Exchange
- `from` - Из / From
- `to` - В / To
- `amount` - Сумма / Amount
- `rate` - Курс / Rate
- `rubles` - Рубли / Rubles
- `chips` - Фишки / Chips

## ✅ Тестирование:

1. Откройте профиль
2. В правом верхнем углу кликните на флаг 🇬🇧
3. Все тексты должны смениться на английский
4. Кликните на 🇷🇺 - вернётся русский
5. Обновите страницу - выбор сохранится

## 🔧 Интеграция в Upgrade:

Для интеграции в upgrade нужно:

1. Добавить в `upgrade/index.html`:
```html
<script src="../i18n.js"></script>
<script src="../language-switcher.js"></script>
```

2. Обновить `upgrade/index.js` - заменить строки на:
```javascript
showToast(window.i18n.t('bet_accepted'));
showToast(window.i18n.t('min_bet') + ' 50₽');
showToast(window.i18n.t('max_bet') + ' 10000₽');
showToast(window.i18n.t('no_chips'));
```

## 📱 Мобильная адаптация:

Переключатель автоматически уменьшается на экранах < 768px:
- Размер кнопок: 32px вместо 36px
- Флаги: 18px вместо 20px
- Padding: 4px вместо 6px

## 💡 Советы:

1. **Короткие ключи:** Используйте краткие, но понятные ключи (например, `bet_amount` вместо `upgrade_bet_amount_label`)

2. **Контекст:** Если одно слово переводится по-разному в разных местах, создайте отдельные ключи:
   - `menu_game` (в меню)
   - `choose_game` (выбор игры)

3. **Тестирование:** Всегда проверяйте на самом длинном переводе (обычно английский)

4. **Fallback:** Если ключ не найден, отображается сам ключ - так легче найти непереведенные места
